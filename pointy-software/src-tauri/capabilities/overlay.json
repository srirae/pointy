{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the push-to-talk pill window. It listens for hotkey/mic events and hides itself once the release animation finishes.",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-ignore-cursor-events"
  ]
}
