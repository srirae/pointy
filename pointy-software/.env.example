# NVIDIA NIM (build.nvidia.com). Read by the Rust backend only — never exposed
# to the webview, so do not add a VITE_ prefix (Vite would inline it into the
# shipped bundle). Copy this file to .env and paste your key.
NVIDIA_API_KEY=
